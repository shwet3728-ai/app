services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shwets-coffee-shop
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/data:/app/backend/data
    restart: unless-stopped
